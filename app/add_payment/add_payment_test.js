'use strict';

describe('GroupExpenseTracker.addPayment module', function() {

  beforeEach(module('GroupExpenseTracker.addPayment'));

  describe('addPayment controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var addPaymentCtrl = $controller('AddPaymentCtrl');
      expect(addPaymentCtrl).toBeDefined();
    }));

  });
});